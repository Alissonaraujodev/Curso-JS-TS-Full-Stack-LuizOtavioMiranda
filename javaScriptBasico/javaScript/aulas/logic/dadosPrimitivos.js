// Tipos primitivos

//string -> conjunto de caracteres.
const nome = 'Luiz';
const nome1 = "Luiz";
const nomw2 = `Luiz`;

//number -> números(inclusive os com pontos flutuantes).
const num = 10;
const num1 =10.52;

// undefined -> não aponta pra local nenhum na memória.
// undefined é quando o valor não é definido.
let nomeAluno; 

// null -> não aponta pra local nenhum na memória.
//null é usado quando a variável não vai apontar para nenhum local na mémoria.
const sobrenomeAluno = null;

//boolean -> verdadeiro ou falso (lógico)
const boolean = true
const boolean1 = false


//NaN -> Not a number(não é um número)(Não é um dado primitivo)
// Quando a operação realizada retorna algo que não é um número
// Por exemplo que se tenta somar um string com number***  

